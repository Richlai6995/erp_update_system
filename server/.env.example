PORT=3000
DB_PATH=system.db
LDAP_URL=ldap://10.8.91.152:389
LDAP_BASE_DN=DC=example,DC=com
LDAP_MANAGER_DN=CN=Manager,DC=example,DC=com
LDAP_MANAGER_PASSWORD=secret

# ERP SSH Configuration (For Compile Commands)
ERP_SSH_HOST=192.168.1.100
ERP_SSH_PORT=22
ERP_SSH_USERNAME=apps
ERP_SSH_PASSWORD=secret

# ERP FTP Configuration (For File Uploads)
# If using SFTP (Secure FTP), this is often same as SSH. 
# If using standard FTP, port is 21.
ERP_FTP_HOST=192.168.1.100
ERP_FTP_PORT=21
ERP_FTP_USERNAME=apps
ERP_FTP_PASSWORD=secret

# Compile Credentials (if different from SSH or specific for Oracle)
# Usually same as SSH user 'apps' for compiling
ERP_COMPILE_USER=apps
ERP_COMPILE_PASSWORD=secret

# Database string for frmcmp (userid=apps/pwd)
ERP_DB_STRING=apps/secret@PROD
